#include "main.h"

/**
 * mul -  checks for checks for a digit (0 through 9).
 * @x: x -  Variable hold integer
 * @y: y - variable hold integer
 * Return: Always 0.
 */
int mul(int x, int y)
{

	int product;

	product = x * y;

	return (product);
}
