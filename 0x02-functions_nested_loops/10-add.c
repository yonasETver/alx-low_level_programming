#include "main.h"
/**
 * add - a function that sum two integers and returns the thire result
 * @num_one: first integer number input
 * @num_two: second integer number input
 * Return: num_one + num_two
 */
int add(int num_one, int num_two)
{
	return (num_one + num_two);
}
